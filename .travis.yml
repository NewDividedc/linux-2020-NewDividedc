language: bash

before_install:
  - sudo apt-get update && sudo apt-get install -y shellcheck jq

script:
  - ls -l
#  - cd chap0x04 && bash test.sh; cd ..
  - bash test.sh
  - ls -l
  - pwd

notifications:
  email:
    on_success: never

